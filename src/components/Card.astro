---
import "@/styles/components/card.css";

const {
    title,
    description,
    alignment = "center",
    icon,
    buttonLabel,
    buttonUrl,
} = Astro.props;

const getIcon = (iconName) => {
    switch (iconName) {
        case "Plus":
            return "+";
        case "Clock":
            return "🕒";
        case "Rocket":
            return "🚀";
        case "Shield":
            return "🛡️";
        case "Check":
            return "✔️";
        case "Chart":
            return "📊";
        default:
            return iconName;
    }
};
---

<div class={`card card-${alignment}`}>
    <div class="card-icon">{getIcon(icon)}</div>
    <h3 class="card-title">{title}</h3>
    <p class="card-description">{description}</p>
    <a href={buttonUrl} class="card-button">{buttonLabel}</a>
</div>
